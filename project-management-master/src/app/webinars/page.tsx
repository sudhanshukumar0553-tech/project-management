import { RoutePlaceholder } from "@/components/RoutePlaceholder";

export default function WebinarsPage() {
  return (
    <RoutePlaceholder
      title="Webinars"
      description="Explore tutorials, demos, and best practices from live sessions."
    />
  );
}