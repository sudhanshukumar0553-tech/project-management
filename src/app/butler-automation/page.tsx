import { RoutePlaceholder } from "@/components/RoutePlaceholder";

export default function ButlerAutomationPage() {
  return (
    <RoutePlaceholder
      title="Automation"
      description="Set rules and automations to reduce repetitive workflow steps."
    />
  );
}