import { RoutePlaceholder } from "@/components/RoutePlaceholder";

export default function PlannerPage() {
  return (
    <RoutePlaceholder
      title="Planner"
      description="Schedule and prioritize work with calendar-friendly planning tools."
    />
  );
}