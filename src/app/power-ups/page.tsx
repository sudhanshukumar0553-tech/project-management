import { RoutePlaceholder } from "@/components/RoutePlaceholder";

export default function PowerUpsPage() {
  return (
    <RoutePlaceholder
      title="Power-Ups"
      description="Connect external apps and integrations to extend board capabilities."
    />
  );
}