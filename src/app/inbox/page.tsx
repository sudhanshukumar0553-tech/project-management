import { RoutePlaceholder } from "@/components/RoutePlaceholder";

export default function InboxPage() {
  return (
    <RoutePlaceholder
      title="Inbox"
      description="Capture quick tasks, notes, and follow-ups in one place."
    />
  );
}