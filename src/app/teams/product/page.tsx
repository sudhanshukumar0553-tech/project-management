import { RoutePlaceholder } from "@/components/RoutePlaceholder";

export default function ProductTeamsPage() {
  return (
    <RoutePlaceholder
      title="Product Management"
      description="Plan roadmaps, prioritize features, and align teams around outcomes."
    />
  );
}