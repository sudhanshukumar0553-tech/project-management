import { RoutePlaceholder } from "@/components/RoutePlaceholder";

export default function EnterprisePlanPage() {
  return (
    <RoutePlaceholder
      title="Enterprise Plan"
      description="Security, governance, and scale features for large organizations."
    />
  );
}